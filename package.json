{
  "name": "conceptio-obsidian",
  "version": "0.1.0",
  "description": "Obsidian plugin — search the Conceptio open-access archive, insert cited notes, keep a vault reading list.",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/0x923041-dotcom/conceptio-obsidian.git"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test && npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.21.0",
    "obsidian": "^1.6.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
